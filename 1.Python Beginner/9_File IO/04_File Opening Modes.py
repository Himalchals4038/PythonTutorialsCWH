''' r = Open for reading.
    w = Open for writing.
    a = Open for appending.
    + = Open for updating.
    rb = Open for reading in binary mode.
    rt = Open for reading in text mode.
'''

# By default when nothing is specified, r is used, more specifically rt.

# Write is used while making a new file to write the contents of the file.

# Append is used to add lines after the original lines in the file.
# It directly prints the lines after the original lines so make sure to add a space after the previous line.