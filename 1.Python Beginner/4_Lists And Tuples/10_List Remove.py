a = [2, 6, 9, 6, 15, 29]
print(a)
a.remove(9)
print(a)
a.remove(6)
print(a)

# remove functions removes the quantity from the list identical to that specified in the bracket.
# when there are 2 items in the list of the same quantity that is to be removed, only first one is removed.

a.clear()
print(a)
# Clear removes all the values of the targeted list and returns an empty list.